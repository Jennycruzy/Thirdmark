services:
  proof-server:
    image: midnightntwrk/proof-server:8.0.3
    container_name: thirdmark-proof-server
    ports:
      - "6300:6300"
    command: ["midnight-proof-server", "--network", "preprod"]
    restart: unless-stopped
